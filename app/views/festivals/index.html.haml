#festivals-index
  %h1.font-weight-bold Festivals
  %h5.d-inline-block Don't see yours?
  .d-inline-block= link_to "Add it!", {action: :new}
  = search_form_for(@q) do |f|
    = f.search_field :name_cont, class: "form-control input-lg d-inline-block"
    = f.submit "SEARCH", class: "btn btn-primary d-inline-block"
  %ul#results
    - @festivals.each do |f|
      %li
        %h4.font-weight-bold= link_to f.name, {action: 'show', id: f.id}

