= render "devise/shared/anyfest"
#login.ml-7
  %h1.font-weight-bold Forgot your password?
  = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
    = render "devise/shared/error_messages", resource: resource
    .field.form-group
      %h5= f.label :email
      %h5= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control"
    .actions
      %h5= f.submit "SUBMIT", class: "btn btn-primary"
  %h5= render "devise/shared/links"
