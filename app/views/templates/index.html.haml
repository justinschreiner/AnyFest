#home
  %h1.font-weight-bold Search Festivals
  = search_form_for(@q, url: root_path) do |f|
    = f.search_field :name_cont, class: "form-control input-xl d-inline-block"
    = f.submit "SEARCH", class: "btn btn-primary d-inline-block"
  - if (params[:q]).present?
    #results
      - @festivals.each do |f|
        %li= link_to f.name, festival_path(f)
  .pl-5.d-inline-block Don't see your festval?
  .d-inline-block= link_to "add a new festival", new_festival_path
  %h1.font-weight-bold Make your Lineup now!
  %ul
    - @templates.each do |tem|
      -if tem.active?
        = link_to(tem) do
          = image_tag(tem.background_image, size: "210x280", class: "d-inline-block mr-4 border border-danger rounded-lg")
  %h1.font-weight-bold Want to make your own Template?
  = link_to "ADD TEMPLATE", new_template_path, class: "btn btn-primary"
