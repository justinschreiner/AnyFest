#show-template
    %h1#name
        = @template.name
    %h2#festival
        = @template.festival.name
    #image
        - if(@template.background_image.attached?)
            =image_tag(url_for(@template.background_image))